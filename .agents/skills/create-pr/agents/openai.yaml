interface:
  display_name: "Create Pull Request"
  short_description: "Create a PR with context and visual evidence"
  default_prompt: "Use $create-pr to prepare and create a pull request for my current changes."
